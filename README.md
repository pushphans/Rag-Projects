# RAG-Projects

A collection of **Retrieval-Augmented Generation (RAG)** pipelines built with **Python 3.12**, **FastAPI**, **LangChain**, and **DeepSeek Chat**.

This monorepo contains two RAG implementations — one for **text** files with a local vector store, and one for **PDF** files with a cloud-based vector store.

## Prerequisites

- Python 3.12+
- API keys set in `.env` file per project

## Getting Started

```bash
# Clone the repo
git clone https://github.com/your-username/Rag-Projects.git
cd Rag-Projects
```

Refer to each sub-project's `README.md` for detailed setup and usage instructions.

## License

MIT
